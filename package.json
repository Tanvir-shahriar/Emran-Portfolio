{
  "name": "emran-portfolio-mern",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MERN stack version of the Emran portfolio website.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace client",
    "start": "npm run start --workspace server",
    "seed": "npm run seed --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
